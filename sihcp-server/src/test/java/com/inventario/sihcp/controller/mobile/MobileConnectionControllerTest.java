package com.inventario.sihcp.controller.mobile;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
@ActiveProfiles("test")
public class MobileConnectionControllerTest {

    @Test
    public void contextLoads() {
        // Este teste verifica se o contexto da aplicação carrega corretamente
        // Se chegou até aqui, significa que a configuração está funcionando
    }
}