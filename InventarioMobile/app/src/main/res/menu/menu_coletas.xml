<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_refresh"
        android:title="Atualizar"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter"
        android:title="Filtrar"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/filter_todos"
                android:title="Todas as coletas"
                android:checkable="true" />
            <item
                android:id="@+id/filter_com_etiqueta"
                android:title="Com etiqueta"
                android:checkable="true" />
            <item
                android:id="@+id/filter_sem_etiqueta"
                android:title="Sem etiqueta"
                android:checkable="true" />
        </menu>
    </item>

    <item
        android:id="@+id/action_export"
        android:icon="@drawable/ic_export"
        android:title="Exportar"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_sync"
        android:icon="@drawable/ic_sync"
        android:title="Sincronizar"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_clear_filters"
        android:icon="@drawable/ic_clear"
        android:title="Limpar Filtros"
        app:showAsAction="never" />

</menu>